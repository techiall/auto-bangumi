{
  "name": "auto-bangumi-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc",
    "check": "npm run build && npm run format:check",
    "dev": "tsx watch src/agent.ts",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "dependencies": {
    "dotenv": "^17.4.2"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "prettier": "^3.9.6",
    "tsx": "^4.23.0",
    "typescript": "^7.0.2"
  }
}
