# Download-server settings for split-machine deployments.

# Basic Auth credentials for the web/API server and remote library agent.
SERVER_USERNAME=admin
SERVER_PASSWORD=bnPj6kT6tfyYMSr68RSBGwLoRqlEjtYW

# Bind the server API to a private LAN/VPN address when using a remote agent.
# Keep 127.0.0.1 when the agent runs on the same host.
SERVER_API_BIND=127.0.0.1
